{% set name = "python-ulid" %}
{% set version = "3.2.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/python_ulid-{{ version }}.tar.gz
  sha256: b1f0d75b49a2dcb5ebca902c05c71080f87845a0e4ed10bc7c8c8a0c0eb2679d

build:
  entry_points:
    - ulid = ulid.__main__:entrypoint
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - hatch-vcs
    - hatch-fancy-pypi-readme
    - pip
  run:
    - python >={{ python_min }}
    - typing_extensions

test:
  imports:
    - ulid
  commands:
    - pip check
    - ulid --help
  requires:
    - pip
    - python {{ python_min }}

about:
  summary: Universally unique lexicographically sortable identifier
  home: https://github.com/mdomke/python-ulid
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rxm7706
